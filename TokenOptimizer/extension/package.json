{
  "name": "cutok-extension",
  "version": "0.1.0",
  "description": "Token Optimizer browser extension — compress prompts locally on claude.ai and chatgpt.com.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "package": "node package.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.9.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "jsdom": "^25.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  }
}
